# 生成报告

## 输出文件

- `申请表信息.txt` (2257 bytes)
- `千牛云呼叫中心系统-代码(前30页).docx` (59125 bytes)
- `千牛云呼叫中心系统-代码(后30页).docx` (59976 bytes)
- `千牛云呼叫中心系统_操作手册.docx` (44066 bytes)

## 警告

- 用户选择暂不截图；操作手册已保留截图预留位置

## DOCX 校验

- 通过 `python-docx` 解析检查：3 个 DOCX 文件均可正常打开并读取正文。
- 通过 ZIP 结构检查：3 个 DOCX 文件均未发现损坏，且包含 `[Content_Types].xml`、`word/document.xml`、`word/styles.xml`。
- 通过内置 `docx-toolkit` OpenXML CLI 业务校验：3 个 DOCX 文件 `isValid=true`，`errors=0`。
- `千牛云呼叫中心系统-代码(前30页).docx`：正文段落 1550，表格 0，页眉为 `千牛云呼叫中心系统 V1.0    第 1 页`。
- `千牛云呼叫中心系统-代码(后30页).docx`：正文段落 1499，表格 0，页眉为 `千牛云呼叫中心系统 V1.0    第 1 页`。
- `千牛云呼叫中心系统_操作手册.docx`：正文段落 147，表格 1，标题为 `千牛云呼叫中心系统操作手册`。
- 校验警告说明：代码材料使用紧凑代码排版，OpenXML 业务校验器提示 7pt 字号低于正文建议范围；操作手册存在少量未引用关系警告。以上均为警告，不是错误，文件结构有效。
